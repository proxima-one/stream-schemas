{
  "name": "@proximaone/stream-schema-fungible-token",
  "version": "0.1.9",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "lib"
  ],
  "repository": "https://github.com/proxima-one/stream-schemas",
  "jest": {
    "preset": "<rootDir>/../../"
  },
  "author": "proxima.one",
  "license": "MIT",
  "scripts": {
    "prepack": "yarn build",
    "dev": "ts-node src/index.ts test",
    "build": "yarn clean && yarn compile && yarn copy-content-files",
    "clean": "rimraf -rf dist && rimraf tsconfig.tsbuildinfo",
    "copy-content-files": "copyfiles -u 1 src/**/*.proto dist/",
    "compile": "tsc --build",
    "test": "yarn jest"
  },
  "dependencies": {
    "@proximaone/stream-schema-base": "^0.1.6"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  },
  "gitHead": "a41633c065e6442490afc270ae852d6ef595e331"
}
