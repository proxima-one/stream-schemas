{
  "name": "@proximaone/stream-schema-mangrove",
  "version": "0.1.2",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "lib"
  ],
  "repository": "https://github.com/proxima-one/proxima-npm",
  "jest": {
    "preset": "<rootDir>/../../"
  },
  "author": "proxima.one",
  "license": "MIT",
  "scripts": {
    "prepack": "yarn build",
    "dev": "ts-node src/index.ts test",
    "build": "yarn clean && yarn compile && yarn copy-content-files",
    "clean": "rimraf -rf dist && rimraf tsconfig.tsbuildinfo",
    "copy-content-files": "copyfiles -u 1 src/**/*.proto dist/",
    "compile": "tsc --build",
    "test": "yarn jest"
  },
  "dependencies": {
    "@proximaone/stream-schema-base": "^0.1.4"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  },
  "gitHead": "3fc547fd1c8606be88c650c38b865fe294743450"
}
